export enum AudioType {
  CAll = "call",
  SONG = "song",
}

export enum Sex {
  MALE = "male",
  FEMALE = "female",
}

export enum Language {
  FR = "fr",
  EN = "en",
  LATIN = "latin",
}

export enum EBirdNameProperty {
  SCIENTIFIC_NAME = "sciName",
  COMMON_NAME = "comName",
  COMMON_NAME_FR = "comNameFr",
}

export enum LoadingState {
  UNLOADED = "sciName",
  LOADING = "1",
  DONE = "2",
}

export enum FavoriteList {
  NORMAL = "Normal list",
  FAVORITE = "Favorite list",
}

export enum DBRegion {
  US_CA = "US-CA",
  CA_QC = "CA-QC",
  US_FL = "US-FL",
  CA_BC = "CA-BC",
  EARTH = "EARTH",
}
